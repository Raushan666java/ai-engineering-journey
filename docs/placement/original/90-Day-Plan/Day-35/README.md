# Day 35 — Machine Learning & AI Patterns

How to run the demos and practice code:

Requirements: JDK 8+ (javac, java)

Compile:

```powershell
cd c:\xampp\htdocs\Placement\Placement-01\90-Day-Plan\Day-35\code
mkdir -Force ..\bin
javac -d ..\bin *.java
```

Run demos:

```powershell
java -cp ..\bin day35.ml.MLPatterns
java -cp ..\bin day35.ml.AdvancedMLPatterns
java -cp ..\bin day35.ml.Day35ComprehensivePractice
```

Next steps:
- Expand `Day35ComprehensivePractice.java` with more solved problems and exercises
- Add unit tests under `tests/` to validate implementations
- Add `Day35InterviewQA.md` expansions for full 150+ questions
- Add a minimal build script and `run-all` harness for CI

Contact: If you'd like, I can expand practice problems now and add tests and CI config.
